interface:
  display_name: "Implement"
  short_description: "Implement and verify a ticket, then prepare its PR"
  default_prompt: "Use $implement to implement the named ticket, verify its conditions, and record the results in its PR."
policy:
  allow_implicit_invocation: false
