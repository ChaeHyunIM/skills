interface:
  display_name: "Verify"
  short_description: "Check remaining PR conditions and record evidence"
  default_prompt: "Use $verify to check the remaining or changed conditions of this PR and reuse valid existing evidence."
policy:
  allow_implicit_invocation: true
